import { createSlice } from '@reduxjs/toolkit';

export const pedidosSlice = createSlice({
    name: 'pedidos',
    initialState: {
        
    },
    reducers: {

    },
});


export const {  } = pedidosSlice.actions;
export default pedidosSlice.reducer;